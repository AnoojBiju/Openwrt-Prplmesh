# AUTO GENERATED FILE
# GENERATED BY generate_ci_yml.py WITH A LIST OF TESTS TO INCLUDE

R4:urx_osp:MAP-4.2.1:
  extends: .certification:R4:urx_osp
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.2.1:urx_osp.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:urx_osp:MAP-4.18.1_ETH_FH24G:
  extends: .certification:R4:urx_osp
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.18.1_ETH_FH24G:urx_osp.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - allow_failure: true
