.certification:R4:haze:
  stage: certification-R4-haze
  extends: .certification-generic-R4
  variables:
    RUN_ON_DEVICE: "haze"
  before_script:
    - tools/deploy_firmware.py --full --device haze --target-name "$RUN_ON_DEVICE" --image "$HAZE_FULLIMAGE" --whm --configuration ci/configuration/certification/R4/whm/haze.sh
    # - tools/deploy_ipk.sh --certification-mode $RUN_ON_DEVICE buildWHM/$RUN_ON_DEVICE/prplmesh.ipk
  tags:
    - certs-tests-R4
    - haze

include:
  - local: "ci/certification/R4/haze.yml"
